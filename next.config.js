/** @type {import('next').NextConfig} */

const nextConfig = {
    output: 'export',
    // reactStrictMode: false
    // experimental: {
    //     serverActions: true,
    //   },
};

module.exports = nextConfig;
